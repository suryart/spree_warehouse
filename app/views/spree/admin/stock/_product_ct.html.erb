<% product.master.active_variant_container_taxons.each_with_index do |vct, vct_index| %>
  <tr>

    <% if product.master.images.any? %>
      <td><%= link_to image_tag(product.master.images.first.attachment.url(:mini)), product.master.images.first.attachment.url(:product)%></td>
    <% else %>
      <td></td>
    <% end %>
    <td>product_ct</td>

    <td><%= vct.container_taxon.name  if vct.container_taxon %></td>
    <td><%= product.sku %></td>
    <td><%= product.name %></td>
    <td></td>
    <td><%= vct.quantity %></td>

    <td id="restocking_<%= product.master.id %>_<%= vct.cont_taxon_id %>">
      <%= button_link_to t(:restock), admin_restocking_path(product.master, :container_taxon_id => vct.cont_taxon_id),  { :id => product.master.id, :icon => 'switch', :remote => true } %>
    </td>

    <td id="destocking_<%= product.master.id %>_<%= vct.cont_taxon_id %>">
      <%= button_link_to t(:destock), admin_destocking_path(product.master, :container_taxon_id => vct.cont_taxon_id), { :id => product.master.id, :icon => 'switch', :remote => true } %>
    </td>

  </tr>
<% end %>